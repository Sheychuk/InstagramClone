import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  name: {
    textAlign: 'center',
    paddingTop: 5,
    paddingBottom: 5,
    width: 64,
  },
});

export default styles;
